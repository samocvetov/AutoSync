# Windows Third-Party Binaries

Place Windows release binaries here before embedding them into:

- `AutoSyncStudio.exe`
- `AutoSyncRenderServer.exe`

See `BUNDLED_BINARIES.md` and `internal/bundles/manifest.json`.
